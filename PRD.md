# PRD — 펭수 스네이크 (Pengsoo Snake)

| 항목 | 내용 |
|---|---|
| 문서 버전 | v1.0 |
| 작성일 | 2026-09-14 |
| 상태 | MVP 범위 확정 |
| 담당 | woosublee |

---

## 1. 개요 및 배경

### 1.1 제품 한 줄 정의
펭수 캐릭터를 머리로 삼아 조작하는, 브라우저에서 설치 없이 바로 즐기는 클래식 스네이크 게임.

### 1.2 배경 / 해결하려는 문제
- 스네이크는 규칙 설명이 필요 없을 만큼 익숙한 게임이지만, 대부분 캐릭터 정체성이 없는 도형으로만 구현되어 있다.
- 친숙한 캐릭터(펭수)를 입히면 동일한 게임 규칙으로도 체류 시간과 재도전 동기를 높일 수 있다.
- 웹 기반 단일 파일로 만들면 설치·로그인·네트워크 없이 링크 하나로 전달 가능하다.

### 1.3 왜 지금 MVP인가
게임성의 핵심(조작감, 캐릭터 표현)이 실제로 재미있는지를 최소 비용으로 검증한다. 사운드·랭킹·모바일 등 확장 기능은 이 검증을 통과한 후에 붙인다.

---

## 2. 목표 및 성공 지표

### 2.1 목표
| 구분 | 목표 |
|---|---|
| G1 | 별도 설치·빌드 없이 브라우저에서 즉시 실행되는 스네이크 게임 제공 |
| G2 | 펭수 캐릭터가 한눈에 인식되는 시각적 정체성 확보 |
| G3 | 점수와 최고 기록을 통해 재도전 동기 부여 |

### 2.2 성공 지표 (MVP 검증 기준)
| 지표 | 기준 |
|---|---|
| 실행 성공률 | `index.html` 더블클릭 또는 정적 호스팅에서 100% 정상 실행 |
| 학습 곡선 | 설명 없이 첫 플레이 시작까지 10초 이내 |
| 재도전율 | 테스트 참가자의 70% 이상이 게임오버 후 1회 이상 재시작 |
| 조작 정확도 | 방향 입력이 의도와 다르게 동작하는 사례 0건 (역주행 버그 포함) |
| 프레임 | 데스크톱 크롬 기준 60fps 유지, 틱 흔들림 없음 |

---

## 3. 타겟 사용자

| 구분 | 설명 |
|---|---|
| 주 사용자 | 펭수에 친숙한 10~30대, 짧은 시간 가볍게 즐길 웹 게임을 찾는 사용자 |
| 사용 환경 | 데스크톱 브라우저(크롬/엣지/사파리 최신 버전), 키보드 입력 가능 |
| 전제 조건 | 게임 규칙에 대한 사전 설명 불필요 (스네이크는 보편적 규칙) |

---

## 4. 사용자 시나리오

### 4.1 첫 플레이
1. 사용자가 링크를 열거나 `index.html`을 실행한다.
2. `READY` 화면에 펭수 머리와 "스페이스바를 눌러 시작" 안내가 표시된다.
3. 스페이스바를 누르면 펭수가 오른쪽으로 이동을 시작한다.
4. 방향키 또는 WASD로 방향을 바꿔 먹이로 이동한다.
5. 먹이를 먹으면 점수가 1 오르고 몸이 한 칸 길어진다.
6. 벽 또는 자기 몸에 부딪히면 `GAME_OVER` 화면과 최종 점수·최고 점수가 표시된다.
7. 스페이스바를 누르면 즉시 재시작한다.

### 4.2 기록 갱신
1. 이전에 플레이한 사용자가 다시 접속한다.
2. 화면 상단에 이전 최고 점수가 표시되어 있다.
3. 플레이 중 현재 점수가 최고 점수를 넘으면 게임오버 시 최고 점수가 갱신 저장된다.

---

## 5. 기능 요구사항

우선순위: **P0 = MVP 필수** / **P1 = MVP 포함하되 후순위 조정 가능**

### 5.1 게임 루프 (P0)

| ID | 요구사항 |
|---|---|
| F-01 | 20×20 그리드 위에서 펭수(뱀)가 일정 간격(틱)마다 한 칸씩 진행 방향으로 이동한다. |
| F-02 | 머리가 먹이 칸에 도달하면 먹이를 획득한다. 점수 +1, 몸 길이 +1. |
| F-03 | 먹이는 획득 즉시 뱀이 점유하지 않은 빈 칸 중 무작위 위치에 재생성된다. |
| F-04 | 머리가 보드 경계 밖으로 나가면 게임오버. (벽 통과 없음) |
| F-05 | 머리가 자기 몸 칸에 진입하면 게임오버. |
| F-06 | 게임 시작 시 뱀 길이는 3칸, 시작 방향은 오른쪽, 위치는 보드 중앙이다. |
| F-07 | 틱 간격은 120ms 고정. (MVP에서는 속도 증가 없음) |

### 5.2 조작 (P0)

| ID | 요구사항 |
|---|---|
| F-10 | 방향키(↑↓←→)와 WASD로 방향을 전환한다. |
| F-11 | 현재 진행 방향의 정반대 방향 입력은 무시한다. (역주행 방지) |
| F-12 | 한 틱 동안 여러 번 입력해도 실제 방향 전환은 틱당 1회만 반영한다. 입력은 큐에 쌓아 순차 처리한다. |
| F-13 | 스페이스바로 `READY`에서 시작, `GAME_OVER`에서 재시작한다. |
| F-14 | 방향키 입력 시 페이지 스크롤이 발생하지 않도록 기본 동작을 막는다. |

> **F-11/F-12 보충**: 단순히 "입력 즉시 방향 변경"으로 구현하면, 한 틱 안에 ↑ → ← 를 빠르게 누를 때 오른쪽으로 가던 뱀이 왼쪽으로 꺾여 자기 몸과 충돌하는 버그가 발생한다. 반드시 입력 큐 방식으로 구현한다.

### 5.3 점수 및 기록 (P0)

| ID | 요구사항 |
|---|---|
| F-20 | 화면 상단에 현재 점수를 상시 표시한다. |
| F-21 | 화면 상단에 최고 점수를 상시 표시한다. |
| F-22 | 최고 점수는 `localStorage`(키: `pengsoo-snake-best`)에 저장한다. |
| F-23 | 게임오버 시 현재 점수가 최고 점수보다 크면 갱신 저장한다. |
| F-24 | `localStorage` 접근이 차단된 환경(시크릿 모드 등)에서도 게임은 정상 동작해야 한다. 저장 실패는 무시하고 진행한다. |

### 5.4 상태 관리 (P0)

| ID | 요구사항 |
|---|---|
| F-30 | 게임 상태는 `READY` / `PLAYING` / `GAME_OVER` 3가지다. |
| F-31 | `READY`: 펭수 머리와 시작 안내 문구, 조작법을 표시한다. |
| F-32 | `PLAYING`: 게임 루프가 동작하며 점수가 실시간 갱신된다. |
| F-33 | `GAME_OVER`: 보드 위에 반투명 오버레이로 최종 점수, 최고 점수, 재시작 안내를 표시한다. |

### 5.5 시각 표현

| ID | 우선순위 | 요구사항 |
|---|---|---|
| F-40 | P0 | 뱀의 **머리**는 펭수 이미지 에셋으로 렌더링한다. (목표 G2의 핵심) |
| F-41 | P0 | 머리는 진행 방향과 무관하게 정면 고정으로 그린다. (MVP 단순화 — 회전/스프라이트 분기 없음) |
| F-42 | P0 | 뱀의 **몸통**은 펭수 팔레트 기반 단색 원으로 그린다. (검정 외곽선 + 흰색 채움) |
| F-43 | P0 | **먹이**는 노란 원형 아이템으로 그린다. (이미지 에셋이 1개뿐이므로 도형으로 처리) |
| F-44 | P1 | 보드에 격자 배경을 옅게 표시해 이동 단위를 인지할 수 있게 한다. |

---

## 6. 화면 구성

```
┌─────────────────────────────────────┐
│  🐧 펭수 스네이크                     │   ← 타이틀
│                                     │
│  점수 12          최고 점수 37       │   ← 스코어 바
│                                     │
│  ┌───────────────────────────────┐  │
│  │                               │  │
│  │        ●                      │  │   ← 먹이(노란 원)
│  │                               │  │
│  │           ○○○[펭수]            │  │   ← 몸통 + 머리
│  │                               │  │
│  │                               │  │
│  └───────────────────────────────┘  │   ← 480×480 캔버스
│                                     │
│  방향키 / WASD 이동 · 스페이스 시작    │   ← 조작 안내
└─────────────────────────────────────┘
```

- 단일 화면 구성. 라우팅·페이지 전환 없음.
- `READY` / `GAME_OVER` 상태는 캔버스 위 오버레이로 표현한다.

---

## 7. 에셋 및 저작권

### 7.1 보유 에셋
| 파일 | 내용 | 사양 |
|---|---|---|
| `24f8aa435a4277dd5bb73fd66fc6b0b9.jpg` | 헤드셋 착용 펭수 얼굴 일러스트 | 720×540, JPG, **배경 불투명 회색** |

### 7.2 배경 투명도 이슈 — **반드시 처리 필요**
보유 이미지는 JPG이며 배경이 회색으로 채워져 있다. 캔버스에 그대로 `drawImage` 하면 펭수 얼굴 주변에 회색 사각형이 함께 렌더링되어 게임 보드를 해친다.

**해결 방안 (택 1)**

| 방안 | 방법 | 평가 |
|---|---|---|
| **A. 원형 클리핑 (권장)** | `ctx.save()` → `ctx.arc()` + `ctx.clip()` → `drawImage()` → `ctx.restore()`. 이미지 중앙의 얼굴 영역만 원형으로 잘라 그린다. | 전처리 불필요, 코드 몇 줄. 헤드셋 좌우 끝은 잘리지만 캐릭터 인식에는 지장 없음. **MVP 채택** |
| B. 보드 배경색 일치 | 게임 보드 배경을 이미지 배경과 동일한 회색(약 `#727272`)으로 설정 | 구현은 가장 쉬우나 보드 색상 선택이 이미지에 종속됨 |
| C. PNG 투명 배경 전처리 | 이미지 편집으로 배경 제거 후 PNG 저장 | 결과물 품질 최상, 그러나 별도 에셋 작업 필요 → MVP 이후 |

> 구현 시 이미지 원본(720×540)에서 얼굴이 차지하는 영역을 `drawImage`의 소스 좌표(sx, sy, sw, sh)로 잘라내어 정사각형에 맞춰 그린다.

### 7.3 저작권
- 펭수는 EBS의 저작물이다. 본 MVP는 **내부 프로토타입 / 학습 목적**으로 한정한다.
- 외부 공개·배포 시에는 EBS 캐릭터 사용 가이드라인 확인 및 사용 승인 절차가 선행되어야 한다.
- 향후 공개 배포가 결정되면, 자체 제작 도트 아트로 에셋을 교체하는 것을 대안으로 검토한다.
- 코드 구조는 이미지 경로를 상수 하나로 관리하여 **에셋 교체가 한 줄 수정으로 가능**하도록 설계한다.

---

## 8. 기술 사양

### 8.1 스택
| 항목 | 선택 |
|---|---|
| 언어 | 바닐라 JavaScript (ES2020+) |
| 렌더링 | Canvas 2D API |
| 구조 | `index.html` 단일 파일 (HTML + CSS + JS 인라인) |
| 빌드 | 없음 |
| 의존성 | 없음 |
| 저장소 | `localStorage` |

**단일 파일 선택 이유**: 빌드·설치·서버 없이 파일 하나로 전달·실행 가능. MVP 검증 속도가 최우선.

### 8.2 게임 파라미터

| 파라미터 | 값 |
|---|---|
| 그리드 크기 | 20 × 20 |
| 셀 크기 | 24px |
| 캔버스 크기 | 480 × 480 |
| 틱 간격 | 120ms |
| 초기 뱀 길이 | 3 |
| 초기 방향 | 오른쪽 |
| 최대 점수 | 397 (전체 칸 - 초기 길이) |

### 8.3 게임 루프 방식
`requestAnimationFrame` + 누적 시간(accumulator) 기반 **고정 타임스텝**을 사용한다.

```
onFrame(now):
  delta = now - lastTime
  accumulator += delta
  while accumulator >= TICK_MS:
      step()              // 게임 상태 1틱 진행
      accumulator -= TICK_MS
  render()
```

> `setInterval`을 쓰지 않는 이유: 브라우저 탭이 비활성화되면 타이머가 지연·누적되어 복귀 시 뱀이 순간이동하거나 즉사할 수 있다. 고정 타임스텝은 프레임률과 무관하게 게임 속도를 일정하게 유지한다. 단, 탭 복귀 시 `accumulator` 폭증을 막기 위해 `delta` 값에 상한(예: 250ms)을 둔다.

### 8.4 주요 자료구조
| 이름 | 형태 | 설명 |
|---|---|---|
| `snake` | `[{x, y}, ...]` | 머리가 `index 0`. 이동은 앞에 `unshift`, 먹이 미획득 시 뒤를 `pop`. |
| `food` | `{x, y}` | 먹이 위치 |
| `direction` | `{x, y}` | 현재 진행 방향 벡터 |
| `inputQueue` | `[{x, y}, ...]` | 대기 중인 방향 입력. 틱마다 앞에서 하나씩 꺼내 적용. |
| `state` | `'READY' \| 'PLAYING' \| 'GAME_OVER'` | 게임 상태 |

### 8.5 지원 환경
- 데스크톱 크롬 / 엣지 / 사파리 / 파이어폭스 최신 버전
- 키보드 입력 필수
- 모바일은 MVP 미지원 (동작은 하되 조작 불가)

---

## 9. Out of Scope (MVP 제외)

명시적으로 이번 범위에서 제외하며, 검증 결과에 따라 차기 버전에서 검토한다.

| 제외 항목 | 사유 |
|---|---|
| 사운드 / BGM / 효과음 | 에셋 확보 필요, 핵심 재미 검증과 무관 |
| 난이도 선택 (속도 단계) | 고정 속도로 먼저 기준 조작감 확보 |
| 모바일 터치 / 스와이프 조작 | 입력 방식 설계에 추가 비용, 1차 타겟은 데스크톱 |
| 서버 기반 랭킹 / 리더보드 | 백엔드·인증 필요, MVP 범위 초과 |
| 아이템 / 파워업 / 장애물 | 클래식 규칙 검증이 우선 |
| 벽 통과(wrap-around) 모드 | 규칙 변형은 기본 규칙 확정 후 |
| 머리 방향 회전 렌더링 | 스프라이트 분기 필요, 시각 품질 개선 항목 |
| 일시정지 기능 | 짧은 플레이 세션 특성상 필요도 낮음 |
| 다국어 / 접근성 대응 | 내부 프로토타입 단계 |

---

## 10. 리스크 및 대응

| # | 리스크 | 영향 | 대응 |
|---|---|---|---|
| R1 | 펭수 이미지 배경이 불투명 회색이라 렌더링 품질 저하 | 높음 | 7.2의 원형 클리핑 방안 적용 (방안 A) |
| R2 | 펭수 저작권 — 외부 공개 시 문제 소지 | 높음 | 내부 프로토타입으로 한정. 공개 전 EBS 승인 절차 필수. 에셋 경로 상수화로 교체 용이성 확보 |
| R3 | 역주행 버그로 인한 억울한 게임오버 | 중간 | F-11/F-12 입력 큐 방식으로 구현. 해당 케이스 테스트 필수 |
| R4 | 탭 비활성화 후 복귀 시 게임 상태 튐 | 중간 | 고정 타임스텝 + delta 상한(250ms) 적용 |
| R5 | 720×540 이미지를 24px 셀에 축소 렌더링 시 뭉개짐 | 낮음 | 얼굴 영역만 잘라 사용. 필요 시 셀 크기 상향 또는 오프스크린 캔버스에 사전 축소 |
| R6 | 이미지 로드 실패 시 머리가 안 보임 | 낮음 | `img.onerror` 시 도형(원)으로 폴백 렌더링 |
| R7 | `localStorage` 차단 환경에서 예외 발생 | 낮음 | F-24 — try/catch로 감싸고 실패 시 무시 |

---

## 11. 향후 확장 (Post-MVP)

우선순위 순으로 검토한다.

1. **모바일 터치 조작** — 스와이프 제스처 + 반응형 캔버스 크기
2. **사운드** — 먹이 획득음, 게임오버음, 음소거 토글
3. **난이도 / 속도 증가** — 점수 구간별 틱 간격 단축
4. **머리 방향 회전** — 진행 방향에 따른 스프라이트 회전
5. **에셋 확장** — 먹이를 참치·물고기 이미지로, 몸통에 펭수 몸 패턴 적용
6. **서버 랭킹** — 닉네임 기반 리더보드
7. **게임 모드** — 벽 통과 모드, 장애물 모드, 타임어택

---

## 12. 변경 이력

| 버전 | 날짜 | 내용 |
|---|---|---|
| v1.0 | 2026-09-14 | 최초 작성. MVP 범위 확정 (게임 루프 + 점수/최고점수) |
